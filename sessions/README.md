# Sessions

Per-date session logs live here, named `YYYY-MM-DD.md`.
Created automatically by `/end` — no need to write these by hand.

The example log in this folder is illustrative; real logs accumulate as you use SS daily.
`scripts/usage_stats.py` (the `/usage` command) parses these filenames to compute streaks.
